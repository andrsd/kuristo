name: test-labels

jobs:
  smoke-test-1:
    description: Quick smoke test
    labels: [smoke, quick]
    steps:
      - run: echo "Smoke test 1"

  smoke-test-2:
    description: Another smoke test
    labels: [smoke]
    steps:
      - run: echo "Smoke test 2"

  integration-test:
    description: Integration test
    labels: [integration, slow]
    steps:
      - run: echo "Integration test"

  free-job:
    description: Free job
    steps:
      - run: echo "free job"

  i-need-free-job:
    description: Job with a label and a dependency
    needs: free-job
    labels: [quick]
    steps:
      - run: echo "I need free job"
