tests:
  test-user-registered-step:
    description: Run test with a user-registered step
    steps:
      - name: Run test
        uses: run_simulation
        with:
          input: input_file.inp
          output: result.out
