name: test-6

jobs:
  exodiff:
    description: Run exodiff
    steps:
      - name: Prepare
        run: |
          echo "Prepare placeholder"

      - name: Run exodiff
        uses: checks/exodiff
        with:
          gold: input_file.inp
          test: result.out
          abs-tol: 1e-17
          fail-on-diff: false
